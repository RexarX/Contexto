#set(USERVER_FEATURE_MONGODB OFF CACHE BOOL "")
#set(USERVER_FEATURE_POSTGRESQL OFF CACHE BOOL "")
#set(USERVER_FEATURE_REDIS OFF CACHE BOOL "")
#set(USERVER_FEATURE_GRPC OFF CACHE BOOL "")
#set(USERVER_FEATURE_CLICKHOUSE OFF CACHE BOOL "")
#set(USERVER_FEATURE_KAFKA OFF CACHE BOOL "")
#set(USERVER_FEATURE_RABBITMQ OFF CACHE BOOL "")
#set(USERVER_FEATURE_UNIVERSAL OFF CACHE BOOL "")
set(USERVER_FEATURE_PATCH_LIBCURL OFF CACHE BOOL "")

add_subdirectory(userver)

userver_setup_environment()

if(DEFINED ENV{PREFIX})
    message(STATUS "Set install prefix: $ENV{PREFIX}")
    file(TO_CMAKE_PATH "$ENV{PREFIX}" PREFIX_PATH)
    set(CMAKE_INSTALL_PREFIX "${PREFIX_PATH}")
endif()

if(BUILD_TESTS)
    userver_testsuite_add_simple()
endif()
